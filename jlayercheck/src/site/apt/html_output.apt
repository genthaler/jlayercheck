  The html output can be generated by using the ant task or using the HTMLOutput
  class. It generates a report that starts with a view of the module dependecies:

[images/screenshot_hierarchy.png] Hierarchy view in the HTML output

  Afterwards it lists the dependency violations and orphaned classes:
  
[images/screenshot_dependencies.png] Dependencies view in the HTML output
